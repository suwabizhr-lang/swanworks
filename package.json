{
  "name": "swanworks",
  "version": "0.1.0",
  "private": true,
  "description": "SWAN WORKS corporate site and preregistration API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/preregister.js && node --check src/analytics.js && node --check public/preregister.js && node --check public/ga4-events.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
